# Tensura-bot (Render deploy)


## Quick start
- Add environment variables (BOT_TOKEN, ADMIN_IDS) in Render dashboard
- Build command: `pip install -r requirements.txt`
- Start command: `python main.py`


## Files created automatically
- `data/*.json` (users, quizzes, events, verses)
- `media/` folders
